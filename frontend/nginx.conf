server {
    listen 80;
    root /usr/share/nginx/html;

    # Client-side routes (e.g. /project/12) have no file on disk; serve the
    # SPA entry point so a page reload doesn't 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
